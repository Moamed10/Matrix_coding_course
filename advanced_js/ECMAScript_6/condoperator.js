let age =24

let alertmsg =  20 <= age <= 25 ? 'you are between 20 and 25' : 'you are good to go'

console.log(alertmsg)

const doIt = false;
const result = doIt ? "Did it!" : "Didn't do it.";

console.log(result)